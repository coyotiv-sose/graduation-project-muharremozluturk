@startuml Expert
!include user_class.puml

class Expert extends User {
  - specialization: String
  - hourlyRate: Number
  - availableSessions: Session[]
  - bookings: Session[]

  + constructor(name, email, phone, specialization, hourlyRate)
  + createSession(startTime, endTime, status, maxParticipants): Session
  + cancelSession(session: Session): void
  + bookSession(session: Session): void
  + cancelBooking(session: Session): void
  + rescheduleSession(session: Session, newStartTime: Date, newEndTime: Date): Session
  + completeSession(session: Session): Session
  + addSessionNotes(session: Session, notes: String): Session
  + checkAndCompleteSessions(): Session[]
  + getCompletedSessions(): Session[]
  + getAverageRating(): Number
  + getExpertInfo(): Object
  + profile: String (getter)
}
@enduml
